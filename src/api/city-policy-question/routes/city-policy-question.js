'use strict';

/**
 * city-policy-question router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::city-policy-question.city-policy-question');
